import {Security} from '../../security';

export class SecurityDividendsPosition {
  idSecuritycurrency: number;
  realDivInterestIncome: number;
  unitsAtEndOfYear: number;
  taxableAmountMainCurrency: number;
  security: Security;
}
