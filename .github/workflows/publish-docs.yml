name: Generate and Publish Backend Documentation

on:
  push:
    paths:
      - 'backend/**'
  workflow_dispatch:
    branches:
      - master

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      # 1. Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up Java 21
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '21'

      # 3. Build all backend modules and install to local repo
      - name: Build & install all backend modules
        run: |
          cd backend
          mvn clean install -DskipTests

      # 4. Generate Javadoc & OpenAPI spec in the server module
      - name: Generate Javadoc & OpenAPI spec
        run: |
          cd backend/grafioschtrader-server
          mvn javadoc:javadoc springdoc-openapi:generate

      # 5. Prepare docs directory for Pages
      - name: Prepare docs directory
        run: |
          rm -rf docs
          mkdir -p docs/swagger

          # Copy Javadoc output
          cp -R backend/grafioschtrader-server/target/site/apidocs/* docs/

          # Copy OpenAPI spec
          cp backend/grafioschtrader-server/target/generated-docs/openapi.json docs/swagger/

          # Download & extract Swagger UI
          wget -qO- https://github.com/swagger-api/swagger-ui/archive/refs/tags/v4.15.5.tar.gz \
            | tar xz --strip-components=1 swagger-ui-4.15.5/dist
          cp -R * docs/swagger/

          # Write the Swagger UI index.html
          cat > docs/swagger/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="en">
            <head>
              <meta charset="UTF-8" />
              <title>API Documentation</title>
              <link rel="stylesheet" href="swagger-ui.css" />
              <link rel="icon" href="favicon-32x32.png" sizes="32x32" />
              <link rel="icon" href="favicon-16x16.png" sizes="16x16" />
              <style> body { margin:0; background:#fafafa; } </style>
            </head>
            <body>
              <div id="swagger-ui"></div>
              <script src="swagger-ui-bundle.js"></script>
              <script src="swagger-ui-standalone-preset.js"></script>
              <script>
                window.onload = function() {
                  SwaggerUIBundle({
                    url: './openapi.json',
                    dom_id: '#swagger-ui',
                    deepLinking: true,
                    presets: [
                      SwaggerUIBundle.presets.apis,
                      SwaggerUIStandalonePreset
                    ],
                    layout: 'StandaloneLayout'
                  });
                };
              </script>
            </body>
          </html>
          EOF

      # 6. Upload the docs folder as a Pages artifact
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy-pages:
    needs: build-docs
    runs-on: ubuntu-latest
    steps:
      # 7. Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
