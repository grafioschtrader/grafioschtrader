<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
      <groupId>grafioschtrader</groupId>
      <artifactId>grafioschtrader</artifactId>
      <version>0.36.7.2</version>
      <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>grafiosch-test-integration</artifactId>
    <!-- name and description are filtered into application.properties and served by /api/actuator/info,
         which the login and registration screens of the frontend library read. -->
    <name>Grafiosch</name>
    <description>Standalone reference application of the reusable Grafiosch libraries</description>
    <packaging>jar</packaging>

   
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <start-class>grafiosch.GrafioschApplication</start-class>
    </properties>

    <dependencies>
		<dependency>
			<groupId>grafioschtrader</groupId>
			<artifactId>grafiosch-base</artifactId>
	        <version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>grafioschtrader</groupId>
			<artifactId>grafiosch-server-base</artifactId>
	        <version>${project.version}</version>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
        <dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
          <groupId>org.ehcache</groupId>
          <artifactId>ehcache</artifactId>
        </dependency>

		<dependency>
			<groupId>org.mariadb.jdbc</groupId>
			<artifactId>mariadb-java-client</artifactId>
			<scope>runtime</scope>
		</dependency>
		
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

		<!-- Shared integration test fixture (grafiosch.test.rest): users.json reader, JWT acquisition and
		     the registration flow that UserResourceTest builds on. Test scoped dependencies are not
		     transitive, so the five artifacts it needs are declared again below. Requires the reactor to
		     be built with -DskipTests; -Dmaven.test.skip=true would install an empty test jar. -->
		<dependency>
			<groupId>grafioschtrader</groupId>
			<artifactId>grafiosch-server-base</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-resttestclient</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.icegreen</groupId>
			<artifactId>greenmail-junit5</artifactId>
			<version>2.1.11</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- ResourceTestSuite is a JUnit Platform @Suite -->
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-suite-api</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-suite-engine</artifactId>
			<scope>test</scope>
		</dependency>
    </dependencies>
    <build>
		<plugins>
			<!-- Code formatting. Configuration lives in the parent pom; run "mvn spotless:apply". -->
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>grafiosch.GrafioschApplication</mainClass>
					<!-- The e2e profile resolves spring.flyway.locations=filesystem:./migration-baseline against
					     the working directory. Pinning it here keeps that path valid regardless of where mvn was
					     started and of whether the plugin forks. -->
					<workingDirectory>${project.basedir}</workingDirectory>
				</configuration>
			</plugin>
        </plugins>
    </build>

</project>
