/**
 * All entities are located here.</br>
 * 
 * Most entities have a surrogate key of the integer data type. This allows
 * generalization when creating, modifying and deleting a record. This surrogate
 * key is an operation for shared data, because the surrogate key is used as a
 * reference in the {@link grafioschtrader.entities.ProposeChangeEntity} class.
 *
 */
package grafioschtrader.entities;