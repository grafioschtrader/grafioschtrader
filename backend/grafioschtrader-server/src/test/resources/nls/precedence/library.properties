# Synthetic bundle used only by NlsModulePrecedenceTest to prove the basename ordering rule itself,
# independent of any text that is actually shipped. Never delivered to a client.
nls.precedence.probe=library
